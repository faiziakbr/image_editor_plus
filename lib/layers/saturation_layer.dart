import 'package:flutter/material.dart';

class SaturationLayer extends StatelessWidget {

  const SaturationLayer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
